const team_name ={
    red_team:'NoPython',
    blue_team:'MemoryLeak',
    green_team:'JavaLovers'
    };
    
    const rate = {
    red_rate: 1200,
    blue_rate: 1150,
    green_rate: 1300
    };
    
    let member_count = 3;
    console.log(team_name.red_team, member_count, rate.red_rate);
    console.log(team_name.blue_team, member_count, rate.blue_rate);
    member_count = 4;
    console.log(team_name.green_team, member_count, rate.green_rate);